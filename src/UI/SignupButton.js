import React from "react";
import classes from "./SignupButton.module.css";

const SignupButton = () => {
  return <button className={classes.SignupButton}>회원가입</button>;
};

export default SignupButton;
