import React from "react";
import classes from "./SignupButton.module.css";

const SignupButton = () => {
  return <button className={classes.SignupButton}>Sign-up</button>;
};

export default SignupButton;
